# Applied migrations (production Supabase)

One line per migration, added when it is run on the live database. A file in
this folder that is missing from this list has NOT been run.

**025 through 029 are now verified too, 2026-08-25.** The older note here said
they were run but never checked from a machine holding the service key, and
that stopped being true: `node migrations/verify.js` ran against production
that day with credentials from `~/dev/compninja-owen/.env` and answered
"Everything present" for everything it names. **030-033** were already
verified 2026-08-19 by the schema query below; the same run re-confirms them.

**The coverage gap closed 2026-08-29.** `verify.js` now names 035
(`portfolio_items.verified_key`), 038 (the three renewal-watch columns plus
`shared_reports.shared_by_name`), 039 (`org_contacts` + its load-bearing
columns) — and three tables it had never listed at all: `broker_bovs` (019),
`broker_csv_mappings` (021) and `org_contacts` (039), which a passing run used
to say nothing about. `test/migrations.test.js` now holds the TABLES list
against the migration files themselves, so a future `create table` missing
from `verify.js` fails the suite instead of waiting for a reader of this
paragraph. **The extended checks have not yet RUN against production** — this
machine's `.env` carries no Supabase credentials (they live only on Render) —
so the rows below still stand on their own evidence until a service-key
machine re-runs `node migrations/verify.js` and updates the "Full schema
verified" line.

**The new checks were proved to FIRE before being trusted**, against a
throwaway PostgREST stub answering PostgREST's own signals (404/`PGRST205`
for a table, 400/`PGRST204` for a column). A healthy stub answered
"Everything present" at 46 tables / 70 columns, exit 0; a stub with all six
new checks absent named each one under the right migration file and exited
1. That is worth knowing because of what it rules out: when the real run
finally happens, a dirty result is the SCHEMA, not a mis-typed table name or
a column check that never ran. A row silently checking nothing is exactly
the failure this file exists to catch, and it is invisible in a passing run.

**The one migration the tool can never see is 037**: it only widens the
`orgs_kind_check` CHECK constraint, and PostgREST exposes no read-only way to
inspect a constraint — proving it would mean inserting a `tenant_rep` row,
and `verify.js`'s contract is that it writes nothing. Its row stands on the
in-session verification recorded there (`pg_get_constraintdef`, zero rows,
2026-08-21). A future constraint-only migration inherits the same limit; its
row should say so.

**Full schema verified 2026-08-25 (through 034 plus both 036s and 040, via `node migrations/verify.js`: 42 tables, 63 columns, all present).** The earlier pass read: **Full schema verified 2026-08-12 (through 023, via `node migrations/verify.js`: 29 tables, 36 columns, all present):** the 2026-08-05 pass below covered through 015; everything through 014 was
confirmed by `node migrations/verify.js`; 015 was run and verified directly in
the SQL editor against `information_schema` and `pg_indexes` (see its row
below), because this machine's `.env` holds no Supabase credentials.

| File | Status on prod | Evidence |
|------|----------------|----------|
| 000-baseline-hand-created.sql | live (created by hand, pre-folder) | app has been storing leads/cache/shares/analytics/submissions since launch |
| 001-comp-corpus.sql | applied (original create, without per-type columns) | corpus rows exist since 2026-07 |
| 002-accounts.sql | applied | accounts live since 2026-07-19 |
| 003-broker-network.sql | applied 2026-07-19 | dated in the server.js comment it came from |
| 004-comp-corpus-per-type-columns.sql | applied late 2026-07 | run when the corpus outage was found; harvesting verified resumed |
| 005-dev-ideas.sql | applied | /dev ideas persist across deploys |
| 006-devlog-overrides.sql | applied | /dev entry edits persist across deploys |
| 007-contacts.sql | applied (verified 2026-08-04) | /contacts is live; table confirmed by the schema query |
| 008-pro-billing.sql | applied ~2026-07-31 (incl. the comp_snapshot drop-not-null) | test checkouts and the $39 unlock wrote rows |
| 009-subject-sizes.sql | applied 2026-08-01 | run + verified via Chrome, RLS on |
| 010-market-pages.sql | live (created by hand) | Address Explorer pages persist since 2026-08-03 |
| 011-guest-search-quota.sql | applied 2026-08-03 | "already run in prod" per server.js/CLAUDE.md |
| 012-search-timings.sql | applied 2026-08-04 | run + verified via Chrome (information_schema returned all 4 columns); re-confirmed 2026-08-05 by `verify.js` |
| 013-broker-vault.sql | applied 2026-08-05 | run in the SQL editor by the owner; `verify.js` reports the full schema present, and a probe insert confirmed all 28 columns and types are accepted by the live table |
| 014-vault-publish-link.sql | applied 2026-08-05 (logged after the fact) | verified via Chrome in the SQL editor: `published_submission_id` exists in `broker_comps` and `broker_comps_published_submission_idx` is in `pg_indexes` — both halves of the file |
| 015-broker-lead-inbox.sql | applied 2026-08-05 | pre-check (duplicate lowercased emails in `broker_profiles`) returned zero rows first; whole file pasted byte-identical (checksum-matched against the committed file) and run in the SQL editor, "Success. No rows returned", so the `leads.id` type guard did not raise. Verified by one `information_schema`/`pg_indexes` query returning all ten objects: tables `broker_coverage` + `lead_intro_requests`, columns `broker_profiles.user_id` (uuid), `leads.size_sqft` (numeric), `leads.id` (bigint), and indexes `broker_profiles_user_id_uidx`, `broker_coverage_market_type_idx`, `lead_intro_requests_user_id_idx`. Backfill matched 0 profiles because `broker_profiles` is still empty |
| 016-broker-comps-star.sql | applied 2026-08-06 | whole file pasted and run in the SQL editor, "Success. No rows returned"; the file is wrapped in begin/commit so a partial apply was not possible. RUN TWICE: the first run was missing `alter table broker_properties enable row level security`, which 013 sets on broker_uploads and broker_comps and 016 had omitted — without it the anon role could read every broker's buildings and markets through PostgREST. The corrected file was re-run whole (it is idempotent throughout: create-if-not-exists, on-conflict-do-nothing, create-or-replace, and enabling RLS twice is a no-op). Verified three ways: the information_schema/pg_indexes query returned all nine objects (table `broker_properties`, column `broker_comps.property_id`, view `broker_comps_reporting`, and the six indexes); `pg_class.relrowsecurity` reported LOCKED for all of broker_comps, broker_uploads and broker_properties; and `node migrations/verify.js` exited clean at 28 tables / 26 columns. The backfill matched 0 rows because the vault is still empty — 0 comps, 0 properties — which is why this was the cheap moment to run it. |
| 017-broker-property-coordinates.sql | applied 2026-08-06 | Run in the SQL editor as the four `add column if not exists` plus the three `add constraint` statements, wrapped in the file's own begin/commit — "Success. No rows returned". The comment blocks were not pasted; they do not execute, and the statements are byte-equivalent to the file with `--` lines stripped. **NOT IDEMPOTENT, unlike 016:** Postgres has no `add constraint if not exists`, so a second run aborts on `broker_properties_latlng_range` already existing and the whole transaction rolls back — harmless, but it is not the no-op the other files are. Verified twice: `node migrations/verify.js` went from naming all four columns as missing to clean at 28 tables / 30 columns; and the checks were proven to ENFORCE rather than merely exist by attempting an insert with `lat=91`, which PostgREST refused with 400 `violates check constraint`. broker_properties held 0 rows, so every existing row trivially satisfied the new constraints. |
| 018-report-sharing.sql | applied 2026-08-06 | Run in the SQL editor (project "Market comp puller") as the executable statements of the file, comments stripped, editor content read back and diffed against the file before running: "Success. No rows returned". Verified in the same session by query, not by `verify.js` — the local `.env` carries no Supabase credentials (they live only on Render), so the script cannot run from this machine. The query confirmed: all four `shared_reports` columns present, `report_viewers` present, `relrowsecurity` **true on both** `shared_reports` (it was already enabled; the migration does not touch it) and `report_viewers`, and — the backward-compatibility check that mattered — all 4 pre-existing `shared_reports` rows backfilled to `visibility = 'public'`, with zero rows at any other visibility. Original note kept below because the ordering rule stays true for any future deployment of this schema. File written 2026-08-06 as Task 2 of client-sharing feature; migration adds columns to shared_reports and creates report_viewers table. **Deploy-before-migrate takes every existing share link offline, not just new ones.** Production auto-deploys from `main`, so merging the sharing routes IS the trigger. Once the new server.js is live and this migration has not yet run: `getShareRecord`'s Supabase read selects the new columns, PostgREST 400s on the unknown columns, and the deliberately fail-closed catch rethrows — so `GET /api/shared` answers 503 for EVERY share, including every legacy public link already mailed to property owners who have no CompNinja account and no way to know the link is temporarily broken. Meanwhile `POST /api/share` falls back to the ephemeral `shared-reports.json` file, which the host wipes on the next deploy, so shares created during the gap are also lost. Migrate-then-deploy is completely safe; the ordering is one-directional — run this migration BEFORE deploying the sharing routes, never after. |
| 020-search-cache-address.sql | applied 2026-08-08 | Run in the SQL editor (project "Market comp puller") as the file's five executable statements (begin/2×alter/create index/commit), comments stripped; the editor buffer was read back before running and matched the file byte for byte (no Monaco auto-close artifact this time): "Success. No rows returned". Verified in the same session by one query returning exactly 3 rows: columns `search_cache.address_key` and `search_cache.prop_type` in `information_schema.columns`, and `search_cache_address_key_idx` in `pg_indexes`. Purely additive and idempotent (add column if not exists / create index if not exists). Deploy-order-safe by design: the code writes these columns in a separate best-effort PATCH after the main cache insert, so either order costs at most the badge, never a cache row. Existing rows stay null and never badge; the 30-day TTL turns them over. |
| 019-broker-bovs.sql | applied 2026-08-08 | Run in the SQL editor (project "Market comp puller") as the file's three executable statements, comments stripped; the editor buffer was read back line by line before running and one Monaco auto-close artifact (a stray trailing `)`) was removed first: "Success. No rows returned". Verified in the same session by a single query returning exactly 16 rows: table `broker_bovs`, index `broker_bovs_user_id_idx`, `pg_class.relrowsecurity` true, and all 13 columns with expected types (`id` uuid, `user_id` uuid, `lead_id` bigint, `market`/`property_type`/`address`/`notes`/`source`/`status` text, `size_sqft` numeric, `received_on` date, `status_changed_at`/`created_at` timestamptz). No backfill by design: seeding happens in JS on first tracker open (see the file's header). Table created empty, so nothing existing could violate the check constraints. |
| 021-broker-csv-mappings.sql | applied 2026-08-10 | Run in the SQL editor (project "Market comp puller") **by the owner**, reported run before the merge was pushed. **Not independently verified from this machine**, unlike 019/020: the local `.env` carries no Supabase credentials (they live only on Render), and no code path exposes the table's existence to an unauthenticated caller, so there was nothing to probe. The file's own commented `information_schema.tables` trailer is the check to run if confirmation is ever wanted. Purely additive and idempotent (`create table if not exists`); table created empty, one row per broker, `user_id` as the primary key. **Deploy-order-safe by design** and the reason this was not blocking: both `getCsvMapping` and `saveCsvMapping` swallow their own errors, so on a deploy without the table a broker gets a fully working column mapper that simply does not remember their mapping, plus two `console.warn` lines. The failure mode is a lost convenience, never a lost upload. |
| 022-tester-passkey.sql | applied 2026-08-10 | Run in the SQL editor (project "Market comp puller") **by the owner**, who ran the file's single `alter table` plus its commented verification query. **Independently verified from this machine** by reading the production schema in the dashboard rather than taking the run on trust: `public.users` now reports **7 columns**, the seventh being `pro_tester` · `bool` · NON-NULLABLE. That check was chosen deliberately over reading the SQL editor result, because the editor had several unrun query tabs open and it was not possible to tell which one had been executed; the schema itself cannot be ambiguous. Purely additive and idempotent (`add column if not exists`), and `not null default false` backfilled all 12 existing rows to "not a tester" in the ALTER itself, so no account gained access from the migration. Was applied AFTER the route shipped, which the file explicitly allows: `getSessionUser` reads a missing column as `undefined` → `false`, so the only cost of the gap was that `/api/redeem-passkey` would have failed for anyone trying it, never a wrong grant. |
| 023-vault-beta.sql | applied 2026-08-12 | Run in the SQL editor (project "Market comp puller") **by the owner**, who ran the file's single `alter table`. **Independently verified from this machine** rather than taken on trust, the same standard 022 set: PostgREST was asked for `users.vault_beta` across all 15 rows and answered 200 with **15 false, 0 true, 0 null**, so the column exists and `not null default false` backfilled every existing account to "no grant" inside the ALTER itself. No account gained access from the migration. Purely additive and idempotent (`add column if not exists`). **The run immediately exposed a code bug rather than a schema one:** with the column present and the grant set on a test account, the vault still refused, because `getSessionUser` returns a deliberately NARROWED user object and `vault_beta` had never been added to it, so `getEntitlements` read `undefined` and every granted broker resolved to no vault with nothing failing anywhere. Fixed the same day, and `test/routes.test.js` now pins the pairing by reading both sides out of server.js. `migrations/verify.js` also gained checks for this column AND for 022's `pro_tester`, neither of which it had ever looked at, so a future unrun grant migration is caught by the tool instead of by a broker. |
| 024-messaging-hub.sql | applied 2026-08-13 | Run in the SQL editor (project "Market comp puller") **by the owner**. Creates the messaging hub's four tables (`hubs`, `hub_participants`, `hub_items`, `hub_messages`) plus their indexes. Purely additive and idempotent (`create table if not exists` throughout); it ALTERs nothing that already existed, so no live table was touched and nothing could be lost by running it. **Independently verified from this machine** rather than taken on trust, the standard 022 and 023 set: `node migrations/verify.js` answered "Everything present" against 33 tables and 42 columns, and that is a real check rather than a table-existence one, because 024's entry in `verify.js` names the load-bearing COLUMNS too (`hub_participants.token_hash`/`removed_at`/`role`, `hub_items.snapshot`/`private`/`source_ref`) and PostgREST answers a select for an unknown column with a 400 — the same signal the app itself would trip over. **Schema is live AHEAD of the code, which is the safe order:** the hub routes are still on `feat/messaging-hub` and unmerged, so nothing reads these tables in production yet. Until that branch lands, the tables simply sit empty. |
| 025-watchlist-digest.sql | applied 2026-08-13 | Run in the SQL editor **by the owner**, who reported both `alter table` statements run, before the code that uses them was merged. **Not independently verified from this machine** (the local `.env` carries no Supabase credentials, they live only on Render) — the same standing of 021, and weaker than 022/023, where a column could be probed through PostgREST. Nothing here is probeable by an unauthenticated caller. Run `node migrations/verify.js` from anywhere holding the service key to turn this from a report into a fact; it now checks `watchlist_items.last_digest_at` and `users.digest_optout` by name. Purely additive and idempotent (`add column if not exists`); `digest_optout` is `not null default false`, so every existing account backfilled to "still receiving", which is the pre-migration behavior rather than a silent mass unsubscribe. `last_digest_at` is deliberately nullable — null means never mailed, and the first digest for a row falls back to its `created_at` so a new watch cannot mail a year of backlog. **Deploy-order-safe in both directions**: the digest route is admin-gated and nothing calls it automatically, so on a deploy without these columns the worst case is that the first manual run 400s and mails nobody. The feed route reads `last_seen_at` exactly as before. |
| 026-analytics-visitor.sql | applied 2026-08-13 | Run in the SQL editor **by the owner**, who reported the `alter table` (three columns) and the `create index` run, before the code was merged. **Not independently verified from this machine**, same reason and same standing as 025; `node migrations/verify.js` checks all three columns by name (`visitor_id`, `user_id`, `plan`). Purely additive and idempotent throughout (`add column if not exists`, `create index if not exists`); all three columns nullable, so every pre-existing row keeps a null and `aggregateStats`' funnel excludes unattributed rows rather than bucketing them as one unknown visitor. **This is the one in this folder whose ordering genuinely mattered, and it is the opposite of the usual case**: the risk was on the WRITE path, not a read. `logEvent`'s INSERT names these columns, PostgREST 400s an insert naming a column that does not exist, and `storeRow` catches that and falls back to the ephemeral `analytics.jsonl` — so a deploy-then-migrate gap would have diverted every analytics event to a file Render erases on the next deploy, while the app stayed perfectly healthy and `/admin` quietly flattened. Run first, as intended. All three columns are `text`, including `user_id` which holds a uuid: `logEvent` writes `""` for "not applicable" across every text dimension, and a `uuid` column would reject that empty string and 400 the whole row, so one consistent convention removes that failure mode at the cost of a cast (`users.id::text = user_id`) at query time. No FK for the same reason account deletion cascades: these rows are a counter, not a record about a person. |
| 027-account-avatar.sql | applied 2026-08-14 | Run in the SQL editor (project "Market comp puller") **by the owner**, who reported Success. **Not independently verified from this machine**, same standing as 025/026 — the local `.env` carries no Supabase credentials (they live only on Render). Run `node migrations/verify.js` from anywhere holding the service key to turn this from a report into a fact; it now checks table `user_avatars` and column `users.avatar_rev` by name. Purely additive and idempotent (`add column if not exists`, `create table if not exists`). Bytes live in `user_avatars`, not on `users`, so `findUserById`'s `SELECT *` never pulls an 80KB data URI; `users.avatar_rev` is a short content hash whose presence means the circle should show a photo. Account deletion cascades the photo (FK on delete cascade). **Schema is live AHEAD of the code, which is the safe order:** until the avatar routes merge, the column and table sit unread. Deploy-then-migrate is also safe: a missing `avatar_rev` reads as `undefined` → `""` (no photo), and a missing table makes PUT fail rather than store a wrong picture. |
| 028-comp-condition.sql | applied 2026-08-17 | Run in the SQL editor (project "Market comp puller") **by the owner**, who reported the statement run. **Not independently verified from this machine**, same standing as 025/026/027 — the local `.env` carries no Supabase credentials (they live only on Render). Run `node migrations/verify.js` from anywhere holding the service key to turn this from a report into a fact; it now names `comp_corpus.condition`. Purely additive and idempotent (`add column if not exists`), one nullable text column, so every existing row keeps a null and reads as "condition not stated" — which is exactly what those rows mean. **Ordering mattered in the usual corpus direction**: `harvestComps` names every ALL_TYPE_COMP_FIELDS column and PostgREST 400s an insert naming an unknown one, and harvest is fire-and-forget, so deploying first would have diverted every harvested comp to the ephemeral `comp-corpus.jsonl` while the log still printed `Comp corpus +N`; `corpusRowsForMarket` names the same columns in its `&select=`, so reads would have returned empty and every market would have looked uncovered. That is the 2026-07 outage's shape. Run first, as intended. The column is quoted in the DDL because `condition` is reserved in the SQL standard (though not in PostgreSQL). |
| 029-vault-lease-rent.sql | applied 2026-08-17 | Run in the SQL editor (project "Market comp puller") **by the owner**, who reported Success. **Not independently verified from this machine**, same standing as 025/026/027/028 — the local `.env` carries no Supabase credentials (they live only on Render), and this session's network policy additionally refused outbound requests to anything but GitHub and the package registries, so neither the database nor the live site was reachable from here at all. Run `node migrations/verify.js` from anywhere holding the service key to turn this from a report into a fact. **Filed as 028 when it was written and renamed to 029 after the fact**: `028-comp-condition.sql` took that number from another session while this branch was open, and both were applied within a day of each other. The rename is a file-naming fix only — the SQL the owner ran is byte-identical to what 029 now holds, and re-running it would be a no-op anyway. Purely additive and idempotent throughout (`add column if not exists`, `create index if not exists`, and both check constraints guarded by a `pg_constraint` lookup — Postgres has no `add constraint if not exists`, the trap 017 hit). **Ordering genuinely mattered here, on the WRITE path**, like 026 and unlike most of this folder: `normalizeRow` puts `rent_psf`/`rent_basis`/`lease_type`/`rent_psf_yr` on the row server.js inserts wholesale, and PostgREST 400s an insert naming an unknown column — so a deploy-then-migrate gap would have refused each broker's ENTIRE spreadsheet, not merely its lease rows. Run first, as intended, with the code still unmerged behind it. Nothing backfilled: every pre-existing row is a comp whose rent was never captured in a column, and all four columns are nullable, so existing comps read exactly as before. The two check constraints are vocabulary guards only (`rent_basis in ('annual','monthly')`, `lease_type in ('NNN','FS','MG')`) — `broker-vault.js` is the real validator; these exist so a direct SQL insert cannot introduce a basis the application has no rule for. |
| 030-enterprise-orgs.sql | applied 2026-08-19 | Run in the SQL editor (project "Market comp puller") **by the owner** on 2026-08-19, who ran all four firm migrations in one sitting and reported Success on each. **VERIFIED the same day** by the combined `information_schema` query handed over with the SQL — it names all four tables plus `shared_reports.org_id`, `org_members.auto_share`/`joined_at`/`removed_at`, `org_comps.comp` and `org_subscriptions.plan`, and returned **0 rows**, which is that query's "everything present" answer. Not verified by `node migrations/verify.js`, which cannot run from a machine without the service key (the local `.env` carries none; they live only on Render) — `verify.js` now names every one of these objects, so a service-key machine settles it in ten seconds. **This one was ordering-critical and was run BEFORE the code deployed, which is the safe direction**: it adds `shared_reports.org_id`, which `getShareRecord` SELECTs by name on every share read, so deploy-first would have 503'd every share link in the world — including legacy public ones already mailed to property owners with no account. **RENUMBERED FROM 028 AT MERGE TIME**: `main` had already claimed 028 (comp condition) and 029 (vault lease rent) while this branch was in flight, and the collision was caught by the pre-deploy check rather than by Postgres, which would have accepted either file under either name. |
| 031-org-auto-share.sql | applied 2026-08-19 | Run in the SQL editor **by the owner** on 2026-08-19, reported Success, in the same sitting as 030/032/033. **Verified** by the same 0-row schema query (see 030), which names this column specifically. One nullable column, `org_members.auto_share`, and the only migration of the four that is survivable deploy-first: org-access.js reads a missing column as `undefined`, which is its "has not chosen" state, so the firm default would simply have applied to everyone until it ran. That is also exactly why `verify.js` names it — a member who had said **never** would silently start following the firm again. Renumbered from 029 at merge time (see 030). |
| 032-org-shared-comps.sql | applied 2026-08-19 | Run in the SQL editor **by the owner** on 2026-08-19, reported Success. **Verified** by the same 0-row schema query (see 030). Creates `org_comps`, the shared-vault copy table, with RLS enabled. Purely additive and deploy-order-safe on its own: no existing read names this table, so deploy-first costs only the feature (every `/api/vault/firm` call 400s at PostgREST). Table created empty, so nothing existing could violate the unique `(org_id, source_comp_id)` constraint. Renumbered from 030 at merge time (see 030). |
| 033-org-billing.sql | applied 2026-08-19 | Run in the SQL editor **by the owner** on 2026-08-19, reported Success. **Verified** by the same 0-row schema query (see 030). Creates `org_subscriptions`, keyed on `org_id`, with RLS enabled. Deploy-order-safe: `firmSeatSubscriptionFor` wraps its read and returns no seat on error, so an unrun 033 resolves every member to their own tier rather than failing. **Inert until `STRIPE_PRICE_FIRM_MONTHLY` is set** — with it unset, firm checkout 503s and the buy control never renders, so seats stay hand-granted via `orgs.seats` and this table simply stays empty. Renumbered from 031 at merge time (see 030). |
| 034-broker-license.sql | applied 2026-08-20 | Run in the SQL editor (project "Market comp puller") **by the owner** on 2026-08-20, immediately BEFORE PR #134 was merged — the safe direction, and the order this one needs. Adds `broker_profiles.license_number` (`text not null default ''`), which `VAULT.canPublishAs` reads to gate both publish routes. Not yet verified by `node migrations/verify.js` (no service key on this machine — see 030); `verify.js` names the column, so a service-key machine settles it in ten seconds. **Blast radius if it had NOT been run**, measured on the branch rather than assumed: `findBrokerProfile`'s SELECT names the column and PostgREST 400s an unknown one, but both of its reads are individually caught and return `null`, so the failure is confined to `/vault` — identity save and publishing refuse, and nothing is lost. `refreshBrokerProfiles` (the public directory, `/broker/<slug>`, market-page listings, Verified-badge attribution) does **not** select the column and is unaffected either way. Existing profiles arrive as `''` and cannot publish until their owner fills the field in, which is the intent: no published vault comp is stranded, because there are none. |
| 035-portfolio-verified-key.sql | applied 2026-08-21 | Run in the SQL editor (project "Market comp puller") **by the owner** on 2026-08-21, reported Success, immediately BEFORE the deploy — the order this one needs. Adds `portfolio_items.verified_key` (nullable `text`) plus a partial index on `(user_id, property_type, verified_key)`, so a saved property is identified by the address the geocoder VERIFIED rather than the one typed (one building typed three ways was three properties with three value histories — see portfolio-match.js). Not yet verified by `node migrations/verify.js` (no service key on this machine — see 030). **Blast radius if it had NOT been run**, checked in the source rather than assumed: `listPortfolio` names the column in its SELECT and `sbRequest` THROWS on PostgREST's 400, so the desk read would 500 and every portfolio save would fail — loudly, as "Couldn't load your portfolio just now", never as an empty desk (the honest-failure line added the same day), and fixed the moment this runs. Purely additive: every pre-035 row keeps a null key and keeps matching on its exact address, which is the unchanged fallback, so nothing needed backfilling and no existing duplicate was merged. |
| 036-bulk-valuations.sql | applied 2026-08-21 | Run in the SQL editor (project "Market comp puller") **by the owner** on 2026-08-21, immediately BEFORE the merge — the order this one needs. Verified in the same session by a single-row query returning five `true`s: both tables exist, `relrowsecurity` is true on both, and all three indexes are in `pg_indexes`. **RENUMBERED FROM 035 AT MERGE TIME**: `main` had already claimed 035 (portfolio-verified-key) while this branch was in flight, exactly as 030 was renumbered from 028 for the same reason; Postgres never saw the filename, so the renumber is bookkeeping only and the DDL that ran is byte-identical to what this file holds. **The first run landed nothing and the reason is worth keeping**: it was pasted into the wrong PROJECT ("agouraninja-cmd's Project", the empty default, not "Market comp puller"), where it reported Success and created the tables in a database nothing reads. The verification is what caught it — `to_regclass` came back false — which is the whole argument for checking rather than trusting a Success message, and for the deploy skill naming the project by ref. The two empty tables it created there were **dropped on 2026-08-23**, so nothing of this incident survives anywhere. Which project the SQL editor was pointed at was proved by CONTENT, not by the breadcrumb: production carries `users`/`comp_corpus`/`leads` and the scratch project held only these two tables, because no database names its own project ref and the dashboard's `main PRODUCTION` branch chip reads identically in both — it is a BRANCH label, not a project one, and mistaking it is the same class of error as trusting the original Success message. The drop ran inside a `do $$` block that re-checked for those three production tables and for a non-zero row count in either table, raising rather than dropping on any of them, so a paste into the wrong tab would have refused instead of destroying live bulk valuation data. Verified afterwards by `to_regclass` returning null for both names and `pg_tables` counting zero tables in `public` (it was 2 before). Nothing in "Market comp puller" was touched. Creates `bulk_jobs` and `bulk_job_items` for the bulk valuation feature (2026-08-21). Purely additive: it touches no existing table and adds no column to one, and every statement is `create ... if not exists`, so a re-run is a no-op. **Blast radius if it is not run**, measured on the branch rather than assumed, and deliberately narrow: nothing outside `/api/bulk*` and `/bulk` names either table, so an unrun migration costs the new feature and only the new feature — unlike 030, whose `shared_reports.org_id` sat in a read on every share. What it costs there is honesty rather than function: `openBulk`'s 503 answers "unavailable right now" when `DB_CONFIGURED` is false, but with Supabase configured and the tables absent the route reaches PostgREST and 400s instead, so the page shows an error rather than the refusal it was written to give. It enables RLS on BOTH tables (added 2026-08-21, before the file was ever run, against 016's scar — 016 shipped without it on `broker_properties` and had to be re-run); `bulk_job_items` holds a member's address list keyed by `user_id`, so verification should check `pg_class.relrowsecurity` and not only that the tables exist. Every statement is `if not exists` or an idempotent `alter ... enable row level security`, so a re-run is a clean no-op — unlike 017. There is **no file fallback by design** (see the migration's own header): a job is a receipt for up to ~$18 of billed searches, and Render erases its disk on every deploy, so a file store would lose a finished portfolio days later with no way to get it back except paying again. |
| 036-org-shop-kind.sql | applied 2026-08-21 (logged after the fact) | Adds `orgs.kind` (`text not null default 'broker'`) plus a guarded `orgs_kind_check`. **Run by another session, which did not log it here**; recorded on 2026-08-21 by a different session after confirming the column with an `information_schema.columns` check against "Market comp puller". Not verified beyond the column — the check constraint was not separately confirmed, and the file is idempotent, so re-running it settles that in seconds. **Why this row is worth having rather than leaving to its author**: `orgsByIds` and `findOrg` both SELECT `kind` BY NAME, and PostgREST 400s an unknown column — `orgsByIds` catches and returns empty (firm names silently vanish from the desk, shares and vault) while `findOrg` does not (three route handlers would 500). So an unlogged row here reads, by this file's own rule, as "firms are broken in production", and it cost one false alarm before anyone checked. **NUMBER COLLISION, deliberately left alone**: this and 036-bulk-valuations.sql are both 036 — they were in flight at the same time and merged an hour apart. Renaming this one is not cosmetic (org-access.js names the file in a comment) and it is not this session's file to rename; both are applied, so the collision costs nothing but the ambiguity recorded here. A future migration should start at 037. |
| 037-org-shop-kind-tenant-rep.sql | applied + verified 2026-08-21 | Widens `orgs_kind_check` to `('broker', 'development', 'tenant_rep')`, the third shop type. **Run in the SQL editor by Owen on 2026-08-21, BEFORE the deploy of PR #156**, and verified in the same session by the file's own query (`pg_get_constraintdef` LIKE `%tenant_rep%`) returning ZERO rows. Verification was asked for explicitly rather than assumed, because of 036-bulk-valuations' scar one row up: that one reported Success into the wrong PROJECT (`agouraninja-cmd's Project`, the empty default) and created nothing where it mattered. **Strictly widening**, so every row that satisfied 036's constraint satisfies this one — nothing to rewrite, nothing to back out, and a re-run is a clean no-op. **Ordering is NOT 036's hazard**: `kind` already exists, so no SELECT names an unknown column and no firm surface goes down if this is late. The narrower cost of being late is that the deploy puts a third `<option>` in two dropdowns while the database still refuses the value they send, so the owner who picks 'Tenant rep shop' gets a 503 from `POST /api/org` — a route that looks like it should have worked. **Changes no existing firm**: `broker` stays the column default and stays what `ORG.kindOf` reads an unrecognized value as, so the only way into `tenant_rep` is somebody choosing it. One `do $$` block rather than a loose DROP + ADD, because a DO block is one statement and therefore one transaction; run loose, a drop without its add leaves the table with no constraint at all. Migration number assigned by the 2026-08-21 divide-and-conquer plan (§7: Owen takes 037-039, Jacob 040+), and it is the 037 that plan's note about the 036 collision asked the next migration to start at. **WITHDRAWN IN CODE 2026-08-31, AND THIS MIGRATION IS DELIBERATELY NOT REVERTED.** The owner dropped the tenant rep shop; `SHOP_KINDS` and both `<option>` rows are gone and `ORG.validateShopKind` refuses the string, so nothing can write it any more. The CHECK stays wide on purpose: narrowing it back would fail on exactly the rows that would make narrowing worth doing (a firm that had already chosen it), and a value no code can send is a value no row can gain — so the constraint has nothing left to protect. Nor is there anything to backfill: `ORG.kindOf` reads an unrecognized kind as `broker`, so a firm still holding `tenant_rep` simply reads the incumbent vocabulary, which is the same rule that let 036 ship without touching pre-036 firms. Do NOT write an 044 to narrow this; if a future kind is added, widen from `('broker', 'development', 'tenant_rep')` as it stands. |
| 038-lease-renewal-watch.sql | applied + verified 2026-08-22 | Adds `broker_comps.lease_expiry`, `.option_notice_date` and `.renewal_notified_at` plus two partial indexes, and `shared_reports.shared_by_name`. **Run in the SQL editor (project "Market comp puller") by Owen on 2026-08-22, BEFORE the deploy of the renewal watch**, and verified in the same session by the file's own combined query returning ZERO rows. Note §7 of the divide-and-conquer plan says JACOB runs all SQL; Owen ran this one himself, as he did 037, so it is already done and must not be handed over again. **This one IS 030's hazard, unlike 037.** The vault's read passes no `select=` and cannot name a column that does not exist, but its WRITE path can and does: `normalizeRow` always emits every field it knows, null included, so an upload, an inline cell edit and a hand-added comp all POST payloads naming `lease_expiry`. PostgREST 400s an insert on an unknown column, and on the upload path that refuses the broker's WHOLE spreadsheet — the shape of the 004 outage. Deploy-then-migrate therefore costs a broker their import, not merely the new feature. **Purely additive and idempotent** (`add column if not exists` / `create index if not exists` throughout), so a re-run is a clean no-op and there is nothing to back out. Every column is nullable except `shared_by_name`, which carries a `not null default ''` so existing rows need no backfill. **`shared_reports.shared_by_name` is a deliberately UNWRITTEN column** riding along on Owen's call so Jacob makes one trip rather than two — the `orgs.share_default` / `hub_items.status` precedent. It exists to fix the deal board's departed-member split (a member who deletes their account currently appears twice, because `org_comps` denormalizes the sharer's name at share time per 032 while `shared_reports` has none and 018 sets `user_id` to null on delete). Nothing reads or writes it yet; the code lands separately. **It cannot be backfilled** — the names belong to accounts already deleted — so it only fixes the split for members who leave after it ships. Migration number follows §7's assignment (Owen 037-039, Jacob 040+); 037 was spent on tenant rep, so the renewal watch is 038 and tenant contacts would be 039. |
| 039-org-contacts.sql | applied + verified 2026-08-22 | Creates `org_contacts` — a firm's own tenant list (name, email, company, notes) — plus two indexes and RLS. **Run in the SQL editor (project "Market comp puller") by Owen on 2026-08-22, BEFORE the deploy of PR #173**, and verified in the same session by the file's own combined query returning ZERO rows. Owen ran this one himself, as he did 037 and 038; §7 of the divide-and-conquer plan assigns SQL to Jacob, so it must not be handed over again. **NOT `contacts`** — migration 007 already created that table (the owner's ADMIN_KEY-gated internal rolodex behind `/contacts`, holding leads CompNinja itself collected). This is a customer's own address book and the two must never meet; the name keeps them apart as well as the scope does. **Ordering is the NARROW hazard, not 030's**: the routes name a table that does not exist, so every `/api/org/contacts` call 400s until this runs, and nothing outside those routes reads it. Being late costs the new feature and only the new feature. **Purely additive** — one new table, nothing altered, every statement `if not exists`, so a re-run is a clean no-op. **RLS is enabled and the verify query CHECKS it** (`pg_class.relrowsecurity`), against 016's scar: that one shipped without RLS on `broker_properties` and had to be re-run. It matters more here than almost anywhere, because this table holds real tenant names and email addresses a customer typed — the most personal data the product stores. **`org_contacts_email_idx` is load-bearing, not an optimization**: the import's upsert names it in `ON CONFLICT (org_id, email)`, and PostgREST requires an index matching those columns EXACTLY. The first draft of this file used `(org_id, lower(email)) where email is not null`, which matches nothing and would have 400'd every import while the test suite passed — case is normalized at write time instead (013's `dedupe_key` argument). NULLs stay distinct on purpose, so a contact with no email is never merged with another. Migration number follows §7's assignment (Owen 037-039, Jacob 040+); 037 went to tenant rep and 038 to the renewal watch, so contacts is the last of Owen's range. |
| 040-hub-note-emails.sql | applied + verified 2026-08-25 | Creates `hub_notify` (the per-person, per-hub notification cursor: `seen_at`, `notified_at`, keyed on `(hub_id, email)`) and `hub_email_prefs` (the off switch, one row per address, absent = on), both with RLS. Purely additive and idempotent (`create table if not exists` throughout); it ALTERs nothing that already existed, so no live table was touched. **Run in the SQL editor (project "Market comp puller") by the owner on 2026-08-25**, reported Success. **Independently verified from this machine**, not taken on trust: `node migrations/verify.js` answered "Everything present" against 42 tables and 63 columns, and 040's entry in `verify.js` names the load-bearing COLUMNS (`hub_notify.seen_at`/`.notified_at`, `hub_email_prefs.notify`), which PostgREST answers with a 400 when one is missing — the same signal the app itself would trip over. **The ON CONFLICT clause was proved separately**, because a table-existence check cannot see it and 024's partial `hub_items` index is the precedent: a deliberate upsert against a nonexistent `hub_id` returned `23503` (foreign key) rather than `42P10` ("no unique or exclusion constraint matching the ON CONFLICT specification"), which is what says the `on_conflict=hub_id,email` the route actually sends will plan. That probe wrote nothing: the FK rejected it. The same probe against `hub_email_prefs` has no FK to fail against, so it DID insert one row for `probe@example.com`, which was deleted immediately; both tables were confirmed empty afterwards. **Schema is live AHEAD of the code**, which is the safe order: the notification routes are on `feat/hub-note-emails` and unmerged, so nothing reads these tables in production yet. Until that branch lands, the tables simply sit empty. |
| 041-org-branding.sql | applied + verified 2026-08-29 | Creates `org_branding` — one branding profile per firm (the fallback a member’s report letterhead uses when they have no personal `branding_profiles` row), with RLS enabled. Purely additive and idempotent (`create table if not exists`; enabling RLS twice is a no-op); it ALTERs nothing, so no live table was touched. **Run in the SQL editor (project "Market comp puller", verified by the breadcrumb AND by the sitting’s other queries answering against production tables) via Claude in Chrome on 2026-08-29, BEFORE the deploy** — the safe direction, though this one is tolerant in both: every read of the table (`findOrgBrandingFor`, the `firm` field on GET /api/branding, the share-snapshot fallback) is deliberately fail-open and degrades to “no firm brand”, so only the new PUT/DELETE /api/org/branding routes 503 until it exists. **Verified in the same session, not taken on trust**: a combined `information_schema.columns` + `pg_class.relrowsecurity` query returned exactly 10 rows — all 9 columns with expected types (`org_id` uuid; the seven text fields; `updated_at` timestamptz) and `rls: true`. The editor buffer was read back before running (one stray Monaco auto-close `)` was removed first — 019’s artifact, again). `verify.js` names the table as of the same commit. Table created empty; nothing existing could reference it, and the FK to `orgs(id)` cascades on firm deletion. |
| 042-vault-undated-deals.sql | applied + verified 2026-08-29 | Drops the NOT NULL on `broker_comps.deal_date` — the one schema change behind the `undated` sentinel (a blank date stays refused by normalizeRow; the literal word stores SQL null). **Run in the SQL editor (project "Market comp puller", confirmed by the breadcrumb) via Claude in Chrome on 2026-08-29, BEFORE the PR merged** — the order this one needs: the upload inserts rows in one PostgREST batch, so a single undated row against the NOT NULL refuses the broker’s whole spreadsheet (the 038 hazard shape). **Verified in the same session, not taken on trust**: `information_schema.columns` answered exactly one row, `deal_date · is_nullable YES`. The statement was typed as ONE line with no parens or quotes, so Monaco had nothing to auto-close; the buffer was read back before running anyway (041’s habit). Purely widening — every existing row already satisfies a nullable column, nothing to back out, and a re-run is a clean no-op. `migrations/verify.js` cannot see nullability (it checks tables and columns through PostgREST), so the file’s own commented query is the re-check. What a null means downstream is enforced in code and pinned by test/vault-undated-run.test.js: excluded from every blend window by SQL semantics, unpublishable, unshareable to a firm (`org_comps.deal_date` deliberately stays NOT NULL). |
| 043-recent-searches.sql | applied 2026-09-01 (owner-run; NOT verified from this checkout) | Creates `recent_searches` (+ two indexes) and adds the nullable `bulk_job_items.recent_item_id`. The schema behind "a portfolio holds the properties you own": every signed-in search used to auto-save into `portfolio_items`, so a desk headed *Your properties* was a log of everything the account had looked up. A SEPARATE table rather than a `kind` column on `portfolio_items`, for 013's reason and `comp_corpus`'s cautionary tale — that table is read in a dozen places and one missed `kind <> 'recent'` puts browsing back on somebody's books with no symptom on screen. **Run by the owner in the SQL editor on 2026-09-01, BEFORE the PR merged.** Purely additive and idempotent (`create table if not exists`, `create index if not exists`, `add column if not exists`); it ALTERs one live table only to add a nullable column, so nothing existing was touched and a re-run is a clean no-op. **Deploy order is SOFT here, unlike 030/035/026** — nothing that predates this reads the table, so deploying first would have cost cross-device recents (the browser keeps its own `propertyHistory.v1` copy and the list still renders) and left a bulk row without its link, with nothing 500-ing at the visitor. **NOT independently verified**: the checkout this was shipped from carries no `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` in `.env`, so `verify.js` could not be run and the claim above rests on the owner's report rather than on a query. `verify.js` names the table and the column as of this commit, so the first run of it from a credentialled checkout is the real check — do that before trusting this row. |
| 044-firm-messaging.sql | applied 2026-09-01 (owner-run; **verified from this checkout** with `node migrations/verify.js`, which answered "Everything present" after having named all five tables as missing beforehand) | Creates the five firm-messaging tables: `msg_threads`, `msg_thread_members`, `msg_messages`, `msg_comps`, `msg_comp_saves` (+ their indexes, including the PARTIAL unique `msg_threads_dm_uidx` that keeps two colleagues from ending up with two direct-message threads). The schema behind `/messages` — firm messaging as its own feature, separate from the client-facing comp hub in 024. **DEPLOY ORDER IS HARD, like 030/035/026 and unlike 043**: every read names its columns in a PostgREST `select=`, and PostgREST 400s an unknown column, so deploying first makes `/messages` answer 503 for everybody. What it does NOT do is break anything else — no existing route reads these tables — so a slip costs the new feature and never the site. Purely additive and idempotent (`create table if not exists`, `create index if not exists`); it ALTERs no existing table, so a re-run is a clean no-op. Two shapes worth knowing before touching it: `msg_threads_dm_uidx` is PARTIAL and therefore CANNOT be used with PostgREST `on_conflict` (42P10 — the bug that once made every hub vault send fail), so server.js reads-then-inserts and catches the 23505 race; and `msg_comps.source_comp_id` is deliberately a bare uuid with NO foreign key, because a message is a record of what was said and deleting the sender's vault comp must not rewrite it. |
| 046-org-buildings.sql | applied 2026-09-02 (run in the SQL editor by Claude at the owner's request, project *Market comp puller*, and verified in the SAME run by `information_schema`/`pg_indexes`/`pg_class` queries — table, indexes and RLS all present, plus `org_contacts.building_id`). **Numbered one lower while in flight**: main merged `045-macro-readings.sql` (PR #263) first, so the file was renamed before merge; contents unchanged, and the tables were created from the file under its old number | Creates `org_buildings` (+ the plain unique index on `(org_id, address_key)` that the add route's `on_conflict` names, and the `(org_id, updated_at desc)` list index) and adds the nullable `org_contacts.building_id`. The schema behind the Workspace's *Buildings* section — Three Spaces slice 3; the plan numbered it 044, which firm messaging took first. **DEPLOY ORDER IS HARD for the buildings routes and soft for everything else**: `orgBuildingRows` names its columns in a PostgREST `select=`, so deploying first makes `GET|POST|DELETE /api/org/buildings` answer 503 and the desk hides the section — and nothing else reads the table, so nothing else is touched (`test/org-buildings-run.test.js` proves that blast radius against a stand-in answering PGRST205). The `org_contacts.building_id` column is read by NO code yet; it exists so slice 5 is a code change rather than a migration, and it must not be named in `orgContactRows`' `select=` until this has run, or every contacts read 400s. Purely additive and idempotent (`create table if not exists`, `create index if not exists`, `add column if not exists`); a re-run is a clean no-op. RLS is enabled and the file's own verify query checks it (016's scar). **Not verifiable from the checkout this was written in**: its `.env` carries no Supabase credentials (043's situation), so the first credentialled `node migrations/verify.js` — which now names the table and its `verified_key`/`lat`/`lng` columns plus `org_contacts.building_id` — is the real check, and it also closes 043's still-open verification. |
| 047-org-building-notes.sql | applied 2026-09-02 (run in the SQL editor by Claude at the owner's request, project *Market comp puller*, and verified in the SAME run by `information_schema`/`pg_indexes`/`pg_class` queries — table, indexes and RLS all present). **Numbered one lower while in flight**: main merged `045-macro-readings.sql` (PR #263) first, so the file was renamed before merge; contents unchanged, and the tables were created from the file under its old number | Creates `org_building_notes` (+ its `(building_id, created_at desc)` index; RLS on; cascades with the building). The schema behind the Notes section of `/building/<id>` — Three Spaces slice 5. **Deploy order is hard for the notes routes and soft for everything else**: `buildingNotes` names the table, so an unrun 047 makes the sheet's boot fail and the page answer its outage wording, and `POST /api/org/buildings/notes` 503s; nothing else reads it. Depends on 046 (`references org_buildings`). Purely additive and idempotent; a re-run is a clean no-op. Not verifiable from the checkout it was written in (no Supabase credentials, 043's situation); `verify.js` names the table and its `building_id`/`body` columns. |
| 048-org-leases.sql | applied 2026-09-02 (run in the SQL editor by Claude at the owner's request, project *Market comp puller*, and verified in the SAME run by `information_schema`/`pg_indexes`/`pg_class` queries — table, indexes and RLS all present). **Numbered one lower while in flight**: main merged `045-macro-readings.sql` (PR #263) first, so the file was renamed before merge; contents unchanged, and the tables were created from the file under its old number | Creates `org_leases` (+ `(org_id, building_id)` and `(org_id, lease_expiry)` indexes; CHECKs on `rent_basis` and `lease_type`; RLS on; cascades with the building). The schema behind the Leases section of `/building/<id>` and the Critical dates strip on `/buildings` — Three Spaces slice 6. **Deploy order is hard for the leases routes and the strip, soft for everything else**: `orgLeaseRows` names the table, so an unrun 048 makes `/api/org/leases` 503, the sheet's boot fail to its outage wording, and the strip on `/buildings` go missing (that read is wrapped, so the list itself still renders); nothing else reads it. Depends on 046 (`references org_buildings`). `renewal_notified_at` is created and NEVER WRITTEN by this code — mail is not wired, deliberately. Purely additive and idempotent. Not verifiable from the checkout it was written in (no Supabase credentials); `verify.js` names the table and its `option_notice_date`/`rent_basis`/`renewal_notified_at` columns. |
| 049-broker-csv-mappings-signature.sql | applied 2026-09-03 (run in the SQL editor by Claude at the owner's request, project *Market comp puller*, and verified in the SAME session by `information_schema.columns` + `pg_constraint`: `signature text default ''` present, primary key now `(user_id, signature)`, the `users` foreign key intact, and the 4 pre-existing rows still there under the empty signature). Run BEFORE PR #274 merged, as its header asks | Adds `broker_csv_mappings.signature` (`VAULT.headerSignature` of the file's normalized header row, computed server-side) and moves the primary key from `(user_id)` to `(user_id, signature)`, so a broker's remembered column mapping is one per FILE SHAPE rather than one per broker — alternating between a CoStar export and their own tracking sheet used to overwrite one mapping with the other on every upload. **Deploy order is soft but the failure is silent**: `getCsvMapping` names `signature` in its SELECT and `saveCsvMapping` upserts on `on_conflict=user_id,signature`, and both halves swallow their errors, so code-before-migration would never fail an upload — it would just stop remembering anything, which nobody reports. The only non-additive statement is the primary-key swap: no row and no column is dropped, and the old key is a prefix of the new one, so every existing row satisfies it. `verify.js` names the column. |
| 051-bulk-job-options.sql | applied 2026-09-04 (run in the SQL editor by Claude at the owner's request, project *Market comp puller*, branch main PRODUCTION; the two `alter table` statements answered "Success. No rows returned", and a second query in a fresh tab against `information_schema.columns` returned exactly the 2 rows — `bulk_jobs.tx_focus` and `bulk_job_items.subject`). The `comment on column` lines in the file were NOT typed into the editor (documentation only; the editor tab stalls after Run, the 049 note's shape). Run BEFORE PR #297 merged, as its header asks | Adds `bulk_jobs.tx_focus text not null default 'both'` (the job's sales / leases / both focus, the single form's Focus select) and `bulk_job_items.subject jsonb` (the row's asking price, NOI, cap rate and per-type details). The schema behind the bulk page taking every input the single-property form did. **Deploy order is soft for a plain run and hard for the new options**: the insert names the two columns only for a non-default run, so a default job still starts against the old schema while a run with a focus or details 400s at PostgREST into "Could not start that run". Purely additive and idempotent; `verify.js` names both columns. |
| 050-broker-property-facts.sql | applied 2026-09-03 (run in the SQL editor by Claude at the owner's request, project *Market comp puller*, and verified in the SAME run by `information_schema.columns`: `broker_properties.facts` present as `jsonb`). Run BEFORE PR #278 merged, as its header asks | Adds `broker_properties.facts jsonb`: what the broker's own deals on one building AGREE on (year built, clear height, units, lot acres, zoning, class; size from sales only), derived in JS by `building-facts.js` and applied at READ time only, so `broker_comps` keeps exactly what was stated on each deal and the export stays stated-only. **Deploy order is soft but the failure is silent**, the 017 shape: `attachPropertyCoords` names `facts` in its SELECT on every vault read and report blend and never throws, so code-before-migration would stop stitching coordinates AND facts onto comps with nothing reported; the derivation PATCH inside `linkVaultProperties` would 400 into its own catch on every upload. Purely additive; no SQL backfill (a pre-050 book derives on its first vault read). `verify.js` names the column. |

## Verification

```bash
node migrations/verify.js
```

Read-only, zero dependencies, needs `SUPABASE_URL` + `SUPABASE_SERVICE_KEY` in
`.env`. It asks PostgREST for zero rows from every expected table **and every
spot-checked column**, and exits non-zero listing whatever is absent, grouped
by the migration that creates it.

**Check columns, not just tables.** The 004 outage is invisible to a
table-existence check: `comp_corpus` existed throughout, ten *columns* were
missing, every insert 400'd for weeks. `verify.js` asks about both.

Last run 2026-08-05: everything through 014 present. 015 was applied after that
run and verified in the SQL editor instead, so the next `verify.js` (on a
machine that has the Supabase credentials) should report everything present
through 015.

Not in CI, deliberately — CI holds no secrets, by design, so that a fork PR
cannot exfiltrate one. This is a local command run around a schema deploy.

<details>
<summary>The hand-run SQL this replaced (still valid in the Supabase editor)</summary>

Last run 2026-08-04: zero rows. **Zero rows returned means the schema is
complete**:

```sql
select t as missing_table
from unnest(array[
  'leads','search_cache','shared_reports','analytics_events',
  'comp_submissions','comp_corpus','users','sessions','portfolio_items',
  'watchlist_items','password_resets','broker_profiles','dev_ideas',
  'devlog_overrides','contacts','subscriptions','branding_profiles',
  'report_purchases','export_usage','stripe_events','subject_sizes',
  'market_pages','guest_search_quota']) as t
where not exists (select 1 from information_schema.tables
                  where table_name = t)
union all
select 'column: ' || tc || '.' || c
from (values
  ('comp_corpus','beds_baths'), ('comp_corpus','zoning'),
  ('comp_submissions','cited_count'), ('dev_ideas','done_at'),
  ('users','stripe_customer_id'), ('leads','source')) as v(tc, c)
where not exists (select 1 from information_schema.columns
                  where table_name = tc and column_name = c);
```

</details>

After a re-run, update the date on the "Full schema verified" line above.

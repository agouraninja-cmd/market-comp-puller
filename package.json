{
  "name": "market-comp-puller",
  "version": "1.0.0",
  "private": true,
  "description": "Single-page web app that pulls commercial real estate comps via the Anthropic API with web search.",
  "main": "server.js",
  "scripts": {
    "prestart": "node --check server.js",
    "start": "node server.js",
    "desktop": "node desktop.js",
    "test": "node --test \"test/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  }
}

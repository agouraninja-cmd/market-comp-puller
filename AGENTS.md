# CompNinja

Product, architecture, env, and deploy rules live in `CLAUDE.md`. Read that; do not duplicate it here.

Cursor-only reminders (shared checkout, git, portable Node) are in `.cursor/rules/`.

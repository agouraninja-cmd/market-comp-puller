# CompNinja

Product, architecture, env, and deploy rules live in `CLAUDE.md` and the per-feature files it indexes under `.claude/rules/` ("Where everything else lives"). Read those; do not duplicate them here.

Cursor-only reminders (shared checkout, git, portable Node) are in `.cursor/rules/`.
